# configs
в этой папке будут размещены конфигурационные файлы стенда по варианту 4 «Школа»:
postgresql.conf — параметры СУБД PostgreSQL
pg_hba.conf — настройки доступа к СУБД
ufw.rules.txt — правила межсетевого экрана
01-static.yaml — пример статической сетевой конфигурации
